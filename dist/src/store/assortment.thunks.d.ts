import { LibstorefrontInnerState } from '@grupakmk/libstorefront';
export declare namespace AssortmentListThunks {
    const getAssortmentList: (customerId: string) => (dispatch: any, getState: () => LibstorefrontInnerState) => Promise<any>;
}
